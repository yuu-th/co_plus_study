// ChatPage - 相談ページ（簡易実装）

import styles from './ChatPage.module.css';

const ChatPage = () => {
  return (
    <div className={styles.page}>
      <h1 className={styles.title}>相談</h1>
      <p className={styles.comingSoon}>相談機能は実装中です</p>
    </div>
  );
};

export default ChatPage;
